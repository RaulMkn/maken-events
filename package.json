{
  "name": "maken-events",
  "version": "1.0.0",
  "private": true,
  "description": "Web de registro y entradas para eventos (maken-events)",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build:frontend": "npm --prefix frontend install --include=dev && npm --prefix frontend run build",
    "build:backend": "npm --prefix backend install --include=dev && npm --prefix backend run build",
    "copy:frontend": "rm -rf backend/public && cp -r frontend/dist backend/public",
    "heroku-postbuild": "npm run build:frontend && npm run copy:frontend && npm run build:backend",
    "start": "node backend/dist/server.js"
  }
}
